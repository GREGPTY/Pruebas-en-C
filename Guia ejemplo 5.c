#include<stdio.h>
#include<string.h>
void main(){
char a[11],;
int x;
strcpy(a,"hola amigo");//es una funcion que almacena una cadena, el primero es la variable y la segunda el contenido
    for(x=0;a[x]!='\0';x=+1) //el '\0' verifica que si eso es cierto detecta que no hay nada, que es basura
        printf("%c\n",a[x]);
        putchar()
getchar();
}
